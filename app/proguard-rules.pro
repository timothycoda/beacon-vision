# Beacon app ProGuard/R8 rules.
# Minification is disabled until core features are stable (see plan).
# The vendor SDK keep rules are supplied by :glasses/consumer-rules.pro.

# Keep Hilt-generated components.
-keepattributes *Annotation*
